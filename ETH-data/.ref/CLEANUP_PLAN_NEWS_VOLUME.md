# Cleanup Plan — News Volume Signal

1. Add the minimal persistent data shape for event news counts.
2. Reuse existing news article data instead of introducing new crawlers or dependencies.
3. Extend API/window typing with one bounded field only.
4. Add one readable chart signal and explanatory copy without expanding scope.
5. Protect with regression tests and run full root verification.
